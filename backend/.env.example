# Copilot-Lite 后端本地配置示例
# 复制为 .env 后按需修改；所有项均有默认值，可不创建本文件直接运行。

# 运行模式：local（SQLite 零依赖）/ cloud（PostgreSQL）
RUN_MODE=local

# 云端模式示例（PostgreSQL）：
# DATABASE_URL=postgresql+asyncpg://copilot:密码@localhost:5432/copilot

# 日志级别：DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO

# 调试模式（SQL 回显等）
DEBUG=false
